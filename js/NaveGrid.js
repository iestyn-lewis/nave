// Requires inclusion of nvBase as well

Nave.registerLayouts("nvGrid", function() {
    var nvBase = Nave.layouts("nvBase");
    return {
        nvGrid : function(obj) {
            var value = obj.value || [];
            var headerRow = obj.headerRow || false;
            
        }
    }
})